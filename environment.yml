name: ect
channels:
  - conda-forge
  - defaults
dependencies:
  # Python
  - python >=3.8
  # Required
  - gdal >=3.0,<3.6.3
  - xcube >= 0.12.1
  - xcube-cci >= 0.9.9
  # Test libs
  - pytest
  - pytest-cov

