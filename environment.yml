name: ect
channels:
  - conda-forge
  - defaults
dependencies:
  # Python
  - python >=3.8
  # Required
  - aiohttp >= 3.6
  - cartopy
  - cftime
  - dask
  - geopandas
  - lxml >= 4.5
  - matplotlib-base
  - nest-asyncio >= 1.3
  - numcodecs
  - numpy <2
  - pandas
  - pydap == 3.3
  - pyproj
  - python-dateutil
  - rioxarray
  - shapely
  - six
  - xarray
  - xcube >= 1.4.0
  - xvec
  # Test libs
  - pytest
  - pytest-cov
