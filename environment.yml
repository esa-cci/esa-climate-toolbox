name: ect
channels:
  - conda-forge
  - defaults
dependencies:
  # Python
  - python >=3.8
  # Required
  - aiohttp >= 3.6
  - cartopy
  - lxml >= 4.5
  - matplotlib
  - nest-asyncio >= 1.3
  - pydap == 3.3
  - xcube >= 1.4.0
  # Test libs
  - pytest
  - pytest-cov