name: ect
channels:
  - conda-forge
  - defaults
dependencies:
  # Python
  - python >=3.8
  # Required
  - aiohttp >= 3.6
  - lxml >= 4.5
  - nest-asyncio >= 1.3
  - pydap == 3.3
  - xcube >= 1.3.0
  # Test libs
  - pytest
  - pytest-cov